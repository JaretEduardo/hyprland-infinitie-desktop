# fallback palette (seeded by `hypr-wallpaper --seed`) — machine-local.
$lock_bg     = rgba(2f2430ff)
$lock_fg     = rgba(f1e2ecff)
$lock_fg_dim = rgba(c2aab8ff)
$lock_inner  = rgba(3a2c37e6)
$lock_outer  = rgba(e6c4d8ff)
