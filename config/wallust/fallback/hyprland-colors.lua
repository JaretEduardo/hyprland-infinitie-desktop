-- fallback palette (seeded by `hypr-wallpaper --seed`) — machine-local.
-- Matches config/wallust/fallback/colors.json. Replaced by a wallust render
-- as soon as `hypr-wallpaper <image>` is run.
return {
  active_border   = { "rgba(e6c4d8ff)", "rgba(c79db4dd)" },
  inactive_border = "rgba(2e2230aa)",
  shadow          = 0xcc1c1119,
}
